# 1장 실습 과제: GPGPU와 WebGPU의 목적

이 챕터는 **개념 챕터**라 코드를 짜지 않습니다. 대신 머릿속 그림이 제대로 잡혔는지 **글로 설명**해 보는 과제입니다. 코드를 외우는 대신, 이후 챕터를 관통하는 사고방식을 자기 말로 정리하는 것이 목표입니다.

아래 칸을 직접 채운 뒤, `README.md`의 설명과 비교해 보세요. (정답을 똑같이 베끼는 게 아니라, **자기 말로 설명할 수 있는지**가 핵심입니다.)

---

## 과제 1. CPU vs GPU 한 문장 정의

이미지의 모든 픽셀에 `out = in * 0.5` 를 적용하는 작업을 예로 들어, CPU(직렬)와 GPU(병렬)가 이 작업을 **어떻게 다르게 처리하는지** 각각 한두 문장으로 설명하세요.

- CPU는 …
- GPU는 …

추가로: "GPU 코어 하나는 CPU 코어보다 느린데도 GPU가 이 작업에서 이긴다"는 말이 왜 모순이 아닌지 설명하세요.

> 내 답:
>
> (여기에 작성)

---

## 과제 2. 직접 그려 보기 — 직렬 vs 병렬 다이어그램

픽셀 6개(픽셀 0~5)에 같은 연산을 적용하는 상황을, **CPU 직렬**과 **GPU 병렬** 두 가지로 직접 그려 보세요. ASCII 또는 Mermaid 어느 쪽이든 좋습니다. (`README.md`의 다이어그램을 닫고, 먼저 스스로 그려 본 뒤 비교하세요.)

- CPU: 픽셀들이 화살표로 이어져야 합니다(하나 끝나야 다음).
- GPU: 픽셀들이 나란히 놓여야 합니다(동시에).

> 내 그림:
>
> ```text
> (여기에 작성)
> ```

---

## 과제 3. 벡터화로 환원하기

"같은 연산을 수많은 데이터에 동시에 적용"을 **벡터에 함수를 원소별로 적용**하는 형태로 써 보세요. 입력 픽셀 벡터를 $\mathbf{x} = (x_0, \dots, x_{N-1})$, 적용할 함수를 $f$ 라 할 때, 결과 벡터 $\mathbf{y}$ 를 수식으로 표현하세요.

그리고 **왜 각 원소의 계산이 서로 독립이어야 동시에(병렬) 처리할 수 있는지**, 반대로 어떤 종류의 계산은 이 틀에 안 맞는지(앞 결과에 의존하는 예) 한 가지 들어 설명하세요.

> 내 답 (수식 + 설명):
>
> (여기에 작성)

---

## 과제 4. WebGPU의 역할 설명

다음 빈칸을 자기 말로 채워, WebGPU가 브라우저에서 하는 일을 한 문단으로 설명하세요.

- WebGPU는 OS마다 다른 네이티브 GPU API(Vulkan/Metal/Direct3D)를 …
- 그래픽뿐 아니라 순수 계산을 위해 … 라는 경로를 표준으로 제공한다.
- 셰이더는 … 라는 언어로 작성한다.
- 코드에서 지원 여부는 … 로 확인한다.

> 내 답:
>
> (여기에 작성)

---

## 과제 5. 한 줄로 잇기 (가장 중요)

이 튜토리얼의 최종 목적지는 비디오 플레이어의 Super Resolution입니다. 다음 키워드를 **모두** 사용해, "저해상도 비디오 프레임"에서 "선명한 고해상도 화면 출력"까지의 흐름을 한 문단으로 이어 설명하세요.

키워드: `저해상도 프레임`, `매초 수십 프레임`, `GPU 병렬`, `compute shader`, `CNN 추론`, `bilinear 확대`, `디테일 보정`, `canvas 출력`

> 내 답:
>
> (여기에 작성)

---

## 스스로 점검

- [ ] CPU 직렬과 GPU 병렬의 차이를 그림 없이도 말로 설명할 수 있다.
- [ ] "같은 연산을 수많은 데이터에 동시에"를 벡터화 관점에서 설명할 수 있다.
- [ ] GPU가 항상 빠른 게 아니라는 점과 그 이유를 안다.
- [ ] WebGPU가 브라우저에서 하는 역할(통일 인터페이스 + compute shader)을 안다.
- [ ] 왜 비디오 SR에 GPU가 필요한지를 "매 프레임 수백만 픽셀"과 엮어 설명할 수 있다.

다 채웠다면 2장 `02-video-frame-pipeline` 으로 넘어가, 이 큰 그림을 실제 `<video>` → `<canvas>` 파이프라인으로 구체화합니다.
