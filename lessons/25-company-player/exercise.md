# 실습 25. 회사 플레이어로 가기 전 — 다리 놓기

이 장은 **실행 코드가 없는 문서 챕터**입니다. `bun run dev` 도, 채울 `src/` TODO 도 없습니다. 대신 README 의 핵심 대조표를 **자기 말로** 다시 쓰고, 데모와 제품을 잇는 머릿속 지도를 직접 채우는 과제입니다. 답은 외부 정답이 아니라 "내가 설명할 수 있는가"를 기준으로 채우세요.

## 과제 1. 핵심 대조표를 내 말로 다시 쓰기

README 의 "이 튜토리얼이 단순화한 것 ↔ 실제 제품의 대응" 표에서 **아무 행 3개**를 골라, 각 행을 아래 형식으로 한 문단씩 직접 적어보세요.

```text
[주제]
- 데모에서 단순화한 것:
- 실제 제품에서 필요한 것:
- 그 차이를 한 문장으로:
```

추천: `색 표현(RGB↔YUV)`, `SR 모델(tiny↔GAN generator)`, `GPU 장애(없음↔device lost)` 세 행. 이 셋이 이 장의 뼈대입니다.

## 과제 2. YUV ↔ RGB, 13장과 잇기

1. README 의 BT.601 RGB→YUV 행렬에서 **Y 를 만드는 행**(첫 행)을 적고, 그게 13장의 luma 가중치 벡터 $\mathbf{w}$ 와 **같은 종류**임을 한 문장으로 설명하세요.
2. 13장이 쓴 **BT.709** 가중치 $(0.2126, 0.7152, 0.0722)$ 와 BT.601 의 $(0.299, 0.587, 0.114)$ 가 **왜 다른지**(둘 다 luma 인데) 자기 말로 적어보세요. 힌트: 색공간이 다르면 행렬이 다르다.
3. (생각만) 4:2:0 영상에서 **Y 만 풀 해상도**이고 색차는 절반인 이유를, "사람 눈" 관점에서 한 문장으로.

## 과제 3. device lost 복구 — 글로 설계

코드를 짜지 말고, **복구 절차를 의사코드/순서도**로 적어보세요. 다음을 반드시 포함:

- `device.lost` 를 어디서 구독하는가
- lost 가 "의도된 종료"인지 "예기치 못한 사고"인지 어떻게 구분하는가
- 사고면 어떤 리소스를 어떤 순서로 다시 만드는가 (adapter → device → 파이프라인 → 텍스처 → weight 버퍼 → 렌더 루프)

> 힌트: 데모처럼 "초기화 코드를 main 에서 한 번 호출"하는 대신, 그 초기화를 **언제든 다시 부를 수 있는 함수 하나**로 묶는 게 핵심입니다. README 의 device lost 순서도를 자기 말로 다시 그려도 됩니다.

## 과제 4. GAN 오해 깨기 (서술형)

다음 문장은 **틀렸습니다.** 어디가 왜 틀렸는지, 그리고 추론 때 실제로 무엇이 도는지 3~4문장으로 바로잡으세요.

> "회사 모델은 GAN 이니까, 영상 재생 중에도 Generator 와 Discriminator 두 신경망이 GPU 에서 같이 돌아간다. 그래서 18·19장의 단순 CNN 추론과는 완전히 다른 새 기술이다."

정답에 반드시 들어가야 할 것: (a) GAN 은 학습 기법이라는 점, (b) 추론 때 Discriminator 는 안 돈다는 점, (c) 실제로 도는 Generator 가 우리 18·19장 CNN(conv·deconv·ReLU)과 같은 골격이라는 점.

## 과제 5. (선택) 모바일 폴백 전략 한 줄 정하기

내가 만든 SR 플레이어를 모바일에 올린다고 상상하고, 아래를 한 줄씩 정해보세요. 정답은 없습니다 — 트레이드오프를 의식하는 게 목적입니다.

- WebGPU 미지원이면: (예: 원본 그대로 재생 / 경고 표시)
- 지원하지만 너무 느리면: (예: Y 채널에만 SR / 해상도 낮춤 / SR 끄기)
- 어댑터 `limits` 가 데스크톱보다 작으면: (예: workgroup 크기·버퍼 크기 줄이기)

## 성공 기준

- 과제 1~4 를 **빈칸 없이** 자기 말로 채웠다.
- 과제 2 에서 13장 luma 와 YUV 의 Y 를 같은 내적으로 연결해 설명했다.
- 과제 4 에서 "추론 때 Discriminator 는 안 돈다 / Generator=CNN" 두 가지를 모두 짚었다.
- README 의 자가 점검 질문 3개에 막힘없이 답할 수 있다.
