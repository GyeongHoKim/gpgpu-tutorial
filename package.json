{
  "name": "gpgpu-tutorial",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "test": "bun test",
    "check:webgpu": "bun run scripts/check-webgpu-support.ts",
    "make:weights": "bun run scripts/make-weights.ts",
    "make:video": "bash scripts/make-sample-video.sh",
    "create-lesson": "bun run scripts/create-lesson.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@webgpu/types": "^0.1.72",
    "typescript": "^7.0.2"
  }
}
